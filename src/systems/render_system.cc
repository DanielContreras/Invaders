#include "systems/render_system.h"
#include "log.h"

void RenderSystem::init() {
  CORE_DEBUG("Render system initialized");
}

void RenderSystem::update(float dt) {
  CORE_DEBUG("Render system update");
}
